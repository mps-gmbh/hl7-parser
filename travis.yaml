language: python
python:
  - "2.7"
before_install:
  - "pip install -U pip"
# command to install dependencies
install:
  - "pip install tox"
# command to run tests
script: tox

notifications:
  email: false
